# URL pública da aplicação (usada em metadata/canonical)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Endpoint da API FastAPI (apps/api). Usado hoje pelo login; o restante das
# telas ainda consome dados mockados em src/features/*/data.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel Analytics — ativo apenas em produção
NEXT_PUBLIC_VERCEL_ANALYTICS=1
